---
- hosts: all
  become: yes

  vars:

  pre_tasks:
    - name: Upgrade all packages to latest
      apt:
        upgrade: dist
        update_cache: yes
        autoremove: yes
        autoclean: yes

  roles:
    - common
    - docker
    - app
    - ssh
